<form method="post" action="{{ path('anima_program_delete', {'id': anima_program.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anima_program.id) }}">
    <button class="btn">Delete</button>
</form>
