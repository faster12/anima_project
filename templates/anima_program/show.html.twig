{% extends 'base.html.twig' %}

{% block title %}AnimaProgram{% endblock %}

{% block body %}
    <h1>AnimaProgram</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anima_program.id }}</td>
            </tr>
            <tr>
                <th>Program_id</th>
                <td>{{ anima_program.programId }}</td>
            </tr>
            <tr>
                <th>Program_name</th>
                <td>{{ anima_program.programName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('anima_program_index') }}">back to list</a>

    <a href="{{ path('anima_program_edit', {'id': anima_program.id}) }}">edit</a>

    {{ include('anima_program/_delete_form.html.twig') }}
{% endblock %}
